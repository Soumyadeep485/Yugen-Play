import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // --- New AnymeX M3 Base Colors ---
  static const Color background = Color(0xFF0F1115);
  static const Color surface = Color(0xFF161920);
  static const Color surfaceVariant = Color(0xFF1C2028);
  static const Color defaultSeed = Color(0xFF8B5CF6);

  // --- Theme Color Tokens ---
  static const Color primary = Color(0xFF8B5CF6);
  static const Color card = Color(0xFF161920);
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white54;
  static const Color success = Color(0xFF4CAF50);
}